import React, { Component } from "react";
import PropTypes from "prop-types";

class {{pascalCase name}} extends Component {
  static propTypes = {
    s: PropTypes.string.isRequired
  };

  state = {};

  componentDidMount() {
    // DOM manipulation, AJAX calls (prefer async actions)
  }

  componentWillReceiveProps(nextProps) {
    // not called on initial render
    // first check if props have changed
    if (nextProps.s !== this.props.s) {
      this.setState({});
    }
  }

  componentDidUpdate(prevProps, prevState) {
    // DOM manipulation
  }

  componentWillUnmount() {
    // cleanup from componentDidMount
  }

  // use ErrorBoundary instead of componentDidCatch

  render() {
    const { s } = this.props;

    return <div>{s}</div>;
  }
}

export default {{pascalCase name}};
