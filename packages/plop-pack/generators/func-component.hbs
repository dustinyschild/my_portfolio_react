import React from "react";
import PropTypes from "prop-types";

const propTypes = {
  s: PropTypes.string.isRequired
};

const {{pascalCase name}} = ({ s }) => {
  return (
    <div>
      {s}
    </div>
  );
};

{{pascalCase name}}.propTypes = propTypes;

export default {{pascalCase name}};
